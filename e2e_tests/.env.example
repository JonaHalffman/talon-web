# Azure AD Application Credentials
# Get these from Azure Portal > App Registrations > Your App

# Your Azure AD Tenant ID
AZURE_TENANT_ID=your-tenant-id-here

# Application (Client) ID
AZURE_CLIENT_ID=your-client-id-here

# Client Secret (generate in Certificates & secrets)
AZURE_CLIENT_SECRET=your-client-secret-here

# For delegated auth (optional - if using user flow instead of app-only)
# O365_USER_EMAIL=your-user@yourdomain.com

# For shared mailbox access with app-only auth
# (Requires mailbox access grant via Exchange admin center)
SHARED_MAILBOX_EMAIL=info@elektrotechnieker.be
